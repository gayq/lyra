/**
 * @fileoverview
 * See `types.ts` for context on these symbols.
 */

import { Symbol_for } from "@/shared/snapshot";

export const FOLIOCLIENTNAME = "folio client global";
export const FOLIOCLIENT = Symbol_for(FOLIOCLIENTNAME);
